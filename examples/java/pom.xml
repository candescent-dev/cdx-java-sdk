<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Examples project coordinates (not published to Maven Central). -->
  <groupId>com.candescent.forge</groupId>
  <artifactId>di-java-examples</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>Candescent DI Java SDK Examples</name>
  <description>Runnable Java examples for the Candescent Digital Insight API</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <okhttp.version>4.12.0</okhttp.version>
    <kotlin.version>1.9.10</kotlin.version>
    <gson.version>2.11.0</gson.version>
    <junit.version>5.10.2</junit.version>
    <jacoco.version>0.8.12</jacoco.version>
    <!-- Default: Maven Central release. Override with -Ddi.sdk.version for local SDK builds. -->
    <di.sdk.version>1.0.0</di.sdk.version>
  </properties>

  <dependencies>
    <!-- Published SDK: com.candescent.forge:di-java-sdk:1.0.0 -->
    <dependency>
      <groupId>com.candescent.forge</groupId>
      <artifactId>di-java-sdk</artifactId>
      <version>1.0.0</version>
    </dependency>
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
      <version>${gson.version}</version>
    </dependency>
    <dependency>
      <groupId>com.squareup.okhttp3</groupId>
      <artifactId>okhttp</artifactId>
      <version>${okhttp.version}</version>
    </dependency>
    <!-- OkHttp 4 is Kotlin; keep stdlib explicit for exec-maven-plugin classpaths. -->
    <dependency>
      <groupId>org.jetbrains.kotlin</groupId>
      <artifactId>kotlin-stdlib-jdk8</artifactId>
      <version>${kotlin.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.squareup.okhttp3</groupId>
      <artifactId>mockwebserver</artifactId>
      <version>${okhttp.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <sourceDirectory>src/main/java</sourceDirectory>
    <testSourceDirectory>test</testSourceDirectory>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <excludes>
            <exclude>test/**</exclude>
          </excludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <argLine>${jacocoAgentArgLine}</argLine>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <configuration>
              <propertyName>jacocoAgentArgLine</propertyName>
              <append>true</append>
            </configuration>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.3.0</version>
        <configuration>
          <!-- OkHttp keeps JVM-wide daemon TaskRunner/Watchdog threads; client.close() shuts down
               per-client resources. Skip exec's 15s daemon-thread cleanup that produces warnings. -->
          <cleanupDaemonThreads>false</cleanupDaemonThreads>
        </configuration>
        <!-- mainClass must come from -Dexec.mainClass (see run-all-java-examples.sh).
             A hardcoded <mainClass> here overrides the CLI and runs AccountsExample for every example. -->
      </plugin>
    </plugins>
  </build>
</project>
